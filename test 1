<!DOCTYPE html>
<html lang="ms">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kehadiran Koko Smart</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); /* Gradient ala Canva */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #333;
        }
        .card {
            background: white;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            text-align: center;
            width: 90%;
            max-width: 400px;
        }
        h2 { color: #4a4a4a; margin-bottom: 1.5rem; }
        input {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 2px solid #eee;
            border-radius: 10px;
            box-sizing: border-box;
        }
        button {
            background: #764ba2;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            transition: 0.3s;
        }
        button:hover { background: #667eea; }
        #status { margin-top: 15px; font-size: 0.9rem; font-weight: bold; }
    </style>
</head>
<body>

<div class="card">
    <h2>📍 Kehadiran Koko</h2>
    <p>Sila masukkan nama dan tekan butang di bawah apabila berada di lokasi.</p>
    <input type="text" id="nama" placeholder="Nama Penuh Guru">
    <button onclick="getLocation()">SEMAK LOKASI & HADIR</button>
    <div id="status"></div>
</div>

<script>
    // GANTI KOORDINAT INI DENGAN KOORDINAT SEKOLAH ANDA
    const schoolLat = 2.9999; 
    const schoolLon = 101.7777;
    const radiusRim = 100; // Meter

    function getLocation() {
        document.getElementById("status").innerHTML = "Sedang menyemak GPS...";
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(checkDistance, showError);
        } else {
            alert("GPS tidak disokong.");
        }
    }

    function checkDistance(position) {
        const userLat = position.coords.latitude;
        const userLon = position.coords.longitude;
        const distance = calculateDistance(userLat, userLon, schoolLat, schoolLon);

        if (distance <= radiusRim) {
            document.getElementById("status").style.color = "green";
            document.getElementById("status").innerHTML = "✅ Anda di lokasi! Menghantar data...";
            // Fungsi hantar ke Google Sheets boleh ditambah di sini
        } else {
            document.getElementById("status").style.color = "red";
            document.getElementById("status").innerHTML = "❌ Gagal: Anda berada " + Math.round(distance) + "m dari lokasi.";
        }
    }

    function calculateDistance(lat1, lon1, lat2, lon2) {
        const R = 6371e3;
        const dLat = (lat2 - lat1) * Math.PI / 180;
        const dLon = (lon2 - lon1) * Math.PI / 180;
        const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
                  Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
                  Math.sin(dLon/2) * Math.sin(dLon/2);
        return R * (2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a)));
    }

    function showError(error) {
        alert("Ralat GPS: Sila benarkan akses lokasi pada pelayar anda.");
    }
</script>

</body>
</html>
